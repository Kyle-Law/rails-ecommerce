<!-- lib/templates/erb/scaffold/show.html.erb.tt -->

<div class="container mx-auto my-8 px-4">
    <p class="text-lg font-semibold">Name:</p>
  <p class="leading-normal"><%= @promotion.name %></p>

    <p class="text-lg font-semibold">Message:</p>
  <p class="leading-normal"><%= @promotion.message %></p>

    <p class="text-lg font-semibold">Min:</p>
  <p class="leading-normal"><%= @promotion.min %></p>

    <p class="text-lg font-semibold">Mixable:</p>
  <p class="leading-normal"><%= @promotion.mixable %></p>

    <p class="text-lg font-semibold">Discount fixed:</p>
  <p class="leading-normal"><%= @promotion.discount_fixed %></p>

    <p class="text-lg font-semibold">Discount percent:</p>
  <p class="leading-normal"><%= @promotion.discount_percent %></p>

    <p class="text-lg font-semibold">Gift:</p>
  <p class="leading-normal"><%= @promotion.gift %></p>

    <p class="text-lg font-semibold">Gift quantity:</p>
  <p class="leading-normal"><%= @promotion.gift_quantity %></p>

    <p class="text-lg font-semibold">Date start:</p>
  <p class="leading-normal"><%= @promotion.date_start %></p>

    <p class="text-lg font-semibold">Date end:</p>
  <p class="leading-normal"><%= @promotion.date_end %></p>

    <p class="text-lg font-semibold">Voucher code:</p>
  <p class="leading-normal"><%= @promotion.voucher_code %></p>

    <div class="flex items-center justify-start mt-6">
    <%= link_to 'Edit', edit_promotion_path(@promotion), class: "btn btn-primary mr-4" %>
    <%= link_to 'Back', promotions_path, class: "btn btn-primary" %>
  </div>
</div>
